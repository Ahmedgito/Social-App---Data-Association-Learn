<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile - <%= user.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-zinc-900 text-white min-h-screen flex flex-col items-center p-6"
  >
    <div class="max-w-xl w-full">
      <!-- Header Section -->
      <div class="flex justify-between items-center mb-6">
        <h1 class="text-3xl font-bold">Welcome, <%= user.name %> 👋</h1>
        <form action="/logout" method="GET">
          <button
            type="submit"
            class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg font-medium"
          >
            Logout
          </button>
        </form>
      </div>

      <!-- Post Creation -->
      <h3 class="text-lg mb-3">Create a post</h3>
      <form action="/post" method="POST" class="space-y-4">
        <textarea
          name="content"
          placeholder="What's on your mind?"
          class="w-full p-3 rounded-lg bg-zinc-800 text-white resize-none"
          rows="5"
          required
        ></textarea>

        <button
          type="submit"
          class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded-lg"
        >
          Post
        </button>
      </form>

      <!-- Posts List -->
      <div class="mt-10">
        <h3 class="text-lg font-semibold mb-4">Your Posts</h3>

        <div class="space-y-4">
          <div class="bg-zinc-800 p-4 rounded-lg">
            <h4 class="text-sm text-blue-400">@username</h4>
            <p class="mt-3 text-white">
              Lorem ipsum dolor sit amet consectetur adipisicing elit. Quis,
              fugiat.
            </p>
            <div class="btns flex gap-4 mt-3">
              <a class="text-red-500 cursor-pointer"> Like </a>
              <a class="text-zinc-500 cursor-pointer"> edit </a>
            </div>
          </div>
        </div>
        
      </div>
    </div>
  </body>
</html>
