<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Public Feed</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-zinc-950 text-white min-h-screen p-4">
    <div class="max-w-4xl mx-auto">
      <!-- Header -->
      <div class="flex flex-col sm:flex-row justify-between items-center mb-8">
        <h1 class="text-4xl font-extrabold">🌐 Public Feed</h1>
        <a
          href="/login"
          class="mt-4 sm:mt-0 bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg font-medium transition-all"
        >
          Login
        </a>
      </div>

      <!-- Posts Feed -->
      <div class="space-y-6">
        <% posts.reverse().forEach(function(post) { %>
          <div class="bg-zinc-900 p-6 rounded-xl shadow hover:shadow-lg transition-all">
            <div class="flex justify-between items-center">
              <div>
                <h4 class="text-blue-400 font-medium">@<%= post.user.username %></h4>
                <p class="text-sm text-zinc-500"><%= post.user.name %></p>
              </div>
              <span class="text-xs text-zinc-400"><%= post.date%></span>
            </div>
            <p class="mt-4 text-lg text-white leading-relaxed break-words">
              <%= post.content %>
            </p>
          </div>
        <% }) %>
      </div>

      <!-- Empty Feed -->
      <% if (posts.length === 0) { %>
        <p class="text-center text-zinc-500 mt-10">No posts yet. Be the first one!</p>
      <% } %>
    </div>
  </body>
</html>
