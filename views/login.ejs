<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Social App - Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-zinc-950 text-white flex items-center justify-center min-h-screen px-4">
    <div class="w-full max-w-sm space-y-6">
      <!-- Login Card -->
      <form
        action="/login"
        method="POST"
        class="bg-zinc-900 border border-zinc-700 rounded-2xl shadow-2xl p-8 space-y-5"
      >
        <h1 class="text-3xl font-bold text-center mb-2 tracking-tight">Welcome Back</h1>
        <p class="text-center text-zinc-400 text-sm mb-4">Login to your account</p>

        <input
          type="email"
          name="email"
          placeholder="Email"
          required
          class="w-full px-4 py-3 bg-zinc-800 text-sm rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 border border-zinc-700"
        />

        <input
          type="password"
          name="password"
          placeholder="Password"
          required
          class="w-full px-4 py-3 bg-zinc-800 text-sm rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 border border-zinc-700"
        />

        <button
          type="submit"
          class="w-full py-3 bg-indigo-600 hover:bg-indigo-500 transition-colors rounded-lg font-semibold text-white text-sm"
        >
          Login
        </button>

        <!-- Register Link -->
        <p class="text-center text-sm text-zinc-400">
          Don't have an account?
          <a href="/" class="text-indigo-400 hover:underline hover:text-indigo-300">
            Register here
          </a>
        </p>
      </form>

      <!-- View Feed Button -->
      <a
        href="/feed"
        class="block text-center w-full py-3 bg-green-600 hover:bg-green-700 rounded-xl font-semibold transition-all shadow-lg"
      >
        🌍 View Public Feed
      </a>
    </div>
  </body>
</html>
